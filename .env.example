# =============================================================================
# AstroEngine API - Variáveis de Ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário.
# =============================================================================

# Porta de exposição da API
PORT=8000

# Nível de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Chaves de API válidas (separadas por vírgula)
# IMPORTANTE: Altere para chaves seguras em produção!
API_KEYS=dev-key-cosmic-suite

# Origens CORS permitidas (separadas por vírgula)
# Em produção, restrinja às origens das suas aplicações
ALLOWED_ORIGINS=*

# Rate limiting (requisições por minuto por chave de API)
RATE_LIMIT_PER_MINUTE=100

# Sistema de casas padrão (placidus, koch, equal, whole_sign, etc.)
DEFAULT_HOUSE_SYSTEM=placidus

# Orbes padrão para aspectos (em graus)
DEFAULT_ORB_CONJUNCTION=8.0
DEFAULT_ORB_OPPOSITION=8.0
DEFAULT_ORB_TRINE=8.0
DEFAULT_ORB_SQUARE=7.0
DEFAULT_ORB_SEXTILE=6.0

# URL do repositório (conformidade AGPL Seção 13)
SOURCE_REPOSITORY=https://github.com/FW2B/astroengine-api
